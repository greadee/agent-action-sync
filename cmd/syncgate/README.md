# syncgate command

This directory is reserved for the foreground agent command. It will load local configuration, initialize storage and identity, start transports, and expose local-only diagnostics.
